from setuptools import setup, find_packages

setup(
    name="codeteam",
    version="0.1.0",
    description="CodeTeam — 多 Agent 协作路由层",
    packages=find_packages(),
    python_requires=">=3.10",
)
